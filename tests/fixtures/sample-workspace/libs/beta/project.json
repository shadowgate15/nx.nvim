{
  "name": "beta",
  "root": "libs/beta",
  "sourceRoot": "libs/beta/src",
  "projectType": "library",
  "tags": ["scope:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "echo beta-build" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "echo beta-test" }
    }
  }
}
